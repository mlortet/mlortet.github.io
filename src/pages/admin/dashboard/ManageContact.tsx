import React from "react";

const ManageContact: React.FC = () => {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        padding: "20px",
      }}
    >
      <h2>Modifier les informations</h2>
      <p>info & contact modifying page + add return button</p>
    </div>
  );
};

export default ManageContact;
